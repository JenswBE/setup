# Firewall
firewall_allow_vpn_routing: true
firewall_additional_ports:
  - comment: SNMP Daemon
    port: 161
    protos: [udp]
    from_networks: [wireguard_ipv4]
  - comment: Tang (https://github.com/latchset/tang)
    port: 7500
    protos: [tcp]
    from_networks: [local_ipv4, local_ipv6]
  - comment: Wireguard
    port: 51821
    protos: [udp]
    from_networks: [any]

# Security
security_autoupdate_reboot_time: "03:00"
