hostnames:
  private:
    eve:
      fqdn: "eve.{{ general_domain_local }}"
      ipv6: false
    fiona_private:
      fqdn: "fiona-private.{{ general_domain_local }}"
      ipv6: false
    kubo_observability:
      fqdn: "kubo-observability.{{ general_domain_local }}"
      ipv6: false
    kubo_private:
      fqdn: "kubo-private.{{ general_domain_local }}"
      ipv6: false
    po:
      fqdn: "po.{{ general_domain_local }}"
      ipv6: false

  public:
    eve:
      fqdn: "eve.{{ general_domain_default }}"
      ipv6: false
    po:
      fqdn: "po.{{ general_domain_default }}"
      ipv6: true

  virbr_nointernet:
    kubo_download:
      fqdn: "kubo-download.virbr-nointernet.{{ general_domain_local }}"
      ipv6: false
    kubo_media:
      fqdn: "kubo-media.virbr-nointernet.{{ general_domain_local }}"
      ipv6: false
    kubo_private:
      fqdn: "kubo-private.virbr-nointernet.{{ general_domain_local }}"
      ipv6: false
    kubo_public:
      fqdn: "kubo-public.virbr-nointernet.{{ general_domain_local }}"
      ipv6: false
