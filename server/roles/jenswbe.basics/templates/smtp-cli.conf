FROM_NAME="{{ jwba_from_name }}"
FROM_EMAIL="{{ jwba_from_email }}"
TO_NAME="{{ jwba_to_name }}"
TO_EMAIL="{{ jwba_to_email }}"
EMAIL_SERVER_HOSTNAME="{{ jwba_server_hostname }}"
PORT_STARTTLS="{{ jwba_server_port_starttls }}"
USERNAME="{{ jwba_server_username }}"
PASSWORD="{{ jwba_server_password }}"
