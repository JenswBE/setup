argument_specs:
  main:
    short_description: Setup a Tang server for NBDE
    options:
      # === REQUIRED ===

      # === OPTIONAL ===
      jwns_server_port:
        type: "int"
        required: false
        description: Port number for Tang server
