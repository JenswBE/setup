http:
  routers:
    sws-wtech:
      entryPoints: ["websecure"]
      tls: { certresolver: le-tls }
      rule: "Host(`{{ general_domain_wtech }}`)"
      service: sws-wtech

  services:
    sws-wtech:
      loadBalancer:
        servers: [{ url: "http://sws-wtech" }]
