http:
  routers:
    sws-bjoetiek:
      entryPoints: ["websecure"]
      tls: { certresolver: le-tls }
      rule: "Host(`{{ general_domain_bjoetiek }}`)"
      service: sws-bjoetiek

  services:
    sws-bjoetiek:
      loadBalancer:
        servers: [{ url: "http://sws-bjoetiek" }]
