http:
  routers:
    jellyfin:
      entryPoints: ["websecure"]
      tls: { certresolver: le-tls }
      rule: Host(`jellyfin.{{ general_domain_default }}`)
      service: jellyfin

  services:
    jellyfin:
      loadBalancer:
        servers: [{ url: "http://jellyfin:8096" }]
