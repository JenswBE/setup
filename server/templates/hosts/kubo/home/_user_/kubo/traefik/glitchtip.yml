http:
  routers:
    glitchtip:
      entryPoints: ["websecure"]
      tls: { certresolver: le-tls }
      rule: Host(`glitchtip.{{ general_domain_default }}`)
      service: glitchtip

  services:
    glitchtip:
      loadBalancer:
        servers: [{ url: "http://glitchtip:8080" }]
