http:
  routers:
    peanut:
      entryPoints: ["websecure"]
      tls: { certresolver: le-dns }
      rule: "Host(`ups.{{ general_domain_local }}`)"
      service: peanut

  services:
    peanut:
      loadBalancer:
        servers: [{ url: "http://peanut:8080" }]
