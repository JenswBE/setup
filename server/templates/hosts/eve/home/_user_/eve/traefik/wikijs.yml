http:
  routers:
    wikijs:
      entryPoints: ["websecure"]
      tls: { certresolver: le-tls }
      rule: Host(`wiki.{{ general_domain_default }}`)
      service: wikijs

  services:
    wikijs:
      loadBalancer:
        servers: [{ url: "http://wikijs:3000" }]
