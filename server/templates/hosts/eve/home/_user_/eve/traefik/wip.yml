http:
  routers:
    wip:
      entryPoints: ["websecure"]
      tls: { certresolver: le-tls }
      rule: Host(`wip.{{ general_domain_default }}`)
      service: wip

  services:
    wip:
      loadBalancer:
        servers: [{ url: "http://wip:8080" }]
