http:
  routers:
    transmission:
      entrypoints: ["webinternal"]
      rule: "Host(`transmission`)"
      service: transmission

  services:
    transmission:
      loadBalancer:
        servers:
          - url: "http://192.168.100.10:8080/"
