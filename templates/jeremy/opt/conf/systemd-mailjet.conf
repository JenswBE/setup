FROM_NAME="{{ ansible_hostname }}"
FROM_EMAIL="host.{{ ansible_hostname }}@{{ general_domain_default }}"
TO_NAME="Admin of {{ ansible_hostname }}"
TO_EMAIL="{{ general_mail_admin }}@{{ general_domain_default }}"
API_KEY_PUBLIC="{{ mailjet_username }}"
API_KEY_PRIVATE="{{ mailjet_password }}"
